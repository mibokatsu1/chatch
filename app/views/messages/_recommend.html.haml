.main__recommend
  %p1
    オススメのチャット
  .recommend
    - @all_users_chats.each_with_index do |all_users_chat, i|

      .recommend__group
        = link_to all_users_chat_path(all_users_chat) do
          .recommend__group__img
            画像
          .recommend__group__name
            = all_users_chat.name
          .recommend__group__message
            コメント
        .recommend__group__tag
          タグ
      - break if i == 5

  .test
    = link_to all_users_chats_path do
      全体公開チャットへリンク